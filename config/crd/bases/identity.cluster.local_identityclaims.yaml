---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: identityclaims.identity.cluster.local
spec:
  group: identity.cluster.local
  names:
    kind: IdentityClaim
    listKind: IdentityClaimList
    plural: identityclaims
    singular: identityclaim
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Current phase
      jsonPath: .status.phase
      name: Phase
      type: string
    - description: Assigned SPIFFE identity
      jsonPath: .status.spiffeId
      name: SPIFFE ID
      type: string
    - description: Secret name
      jsonPath: .status.secretName
      name: Secret
      type: string
    - description: Certificate expiration
      jsonPath: .status.expiresAt
      name: Expires
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: IdentityClaim is the Schema for the identityclaims API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of IdentityClaim
            properties:
              issuerRef:
                description: issuerRef overrides the default certificate issuer.
                properties:
                  group:
                    default: cert-manager.io
                    description: group of the issuer.
                    type: string
                  kind:
                    default: ClusterIssuer
                    description: kind of the issuer (Issuer or ClusterIssuer).
                    type: string
                  name:
                    description: name of the issuer resource.
                    type: string
                required:
                - name
                type: object
              selector:
                description: |-
                  selector specifies which pods should receive the identity.
                  Pods matching these labels will have access to the generated TLS certificate.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              ttl:
                default: 1h
                description: |-
                  ttl specifies how long the certificate should be valid.
                  Defaults to 1h if not specified. Must be between 5m and 8760h.
                format: duration
                type: string
                x-kubernetes-validations:
                - message: TTL must be between 5m and 8760h
                  rule: duration(self) >= duration('5m') && duration(self) <= duration('8760h')
            required:
            - selector
            type: object
          status:
            description: status defines the observed state of IdentityClaim
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the IdentityClaim resource.
                  Condition types: Ready, CertificateIssued, PodsVerified
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              expiresAt:
                description: expiresAt is the timestamp when the current certificate
                  expires.
                format: date-time
                type: string
              phase:
                description: phase represents the current lifecycle phase of the identity
                  claim.
                enum:
                - Pending
                - Issuing
                - Ready
                - Failed
                type: string
              secretName:
                description: secretName is the name of the Secret containing the TLS
                  certificate.
                type: string
              spiffeId:
                description: |-
                  spiffeId is the SPIFFE identity URI assigned to this claim.
                  Format: spiffe://cluster.local/ns/<namespace>/ic/<name>
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
