{{- if .Values.leaderElection.enabled -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "identity-claim-operator.fullname" . }}-leader-election-rolebinding
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "identity-claim-operator.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "identity-claim-operator.leaderElectionRoleName" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "identity-claim-operator.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end }}
